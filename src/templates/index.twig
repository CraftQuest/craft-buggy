{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Buggy plugin for Craft CMS 3.x
 *
 * Buggy index.twig
 *
 * @author    Ryan Irelan
 * @copyright Copyright (c) 2021 Ryan Irelan
 * @link      https://craftquest.io
 * @package   Buggy
 * @since     1.0.0
 */
#}

{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("craftquest\\buggy\\assetbundles\\buggy\\BuggyAsset") %}
{% do view.registerAssetBundle("craftquest\\buggy\\assetbundles\\indexcpsection\\IndexCPSectionAsset") %}

{# Link for the ? icon at the bottom of the page #}
{% set docsUrl = "https://github.com/ryanirelan/buggy/blob/master/README.md" %}

{# The title of this CP section #}
{% set title = "Buggy" %}

{# The URL to this plugin's base CP section #}
{% set pluginCpUrl = url('buggy') %}

{# Get a URL to an image in our AssetBundle #}
{% set iconUrl = view.getAssetManager().getPublishedUrl('@craftquest/buggy/assetbundles/indexcpsection/dist', true) ~ '/img/Index-icon.svg' %}

{# Content that should appear in the page header#}
{% set extraPageHeaderHtml %}
    <div class="buttons">
        <a href="{{ pluginCpUrl }}" class="btn submit add icon">{{ "Click Me!"|t('buggy') }}</a>
    </div>
{% endset %}

{# The content of the CP Section#}
{% set content %}
    <h2>{{ "Index CP Section body goes here"|t('buggy') }}</h2>
    <img src="{{ iconUrl }}" height="64" width="64" />
    <p class="textline"></p>
{% endset %}
